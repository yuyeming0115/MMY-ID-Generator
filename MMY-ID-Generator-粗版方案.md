# MMY-ID-Generator —— 时装职业装渲染 ID 对照工具（设计报告 / 实施计划）

> 状态：计划待评审（Plan Mode）。本文即「中文 MD + HTML 报告」的内容骨架；审批通过后，会把同内容生成独立 `.md` 与 `.html` 报告文件，并正式开工。
> 数据来源：`E:/下载/ID规范.xlsx`（仅「公式」sheet）+ `E:/下载/3D模型动画资源进度表.xlsx`（仅「SVN时装职业装提交记录」sheet）。

---

## 1. 应用定位与命名

- **英文名：`MMY-ID-Generator`**（遵循你 `MMY-FrameQuickView` 一类纯英文 + MMY- 前缀风格）
- **中文副标题（仅文档/UI 标题用，非项目名）：昆仑时装职业装 ID 对照器**
- **一句话定位**：把你"看不懂、记不住"的渲染输出 ID，变成「输入即出中文名 + 自动列出身体/头发/武器 + 关联套件 + 可迭代编辑」的本地小工具。
- **解决的核心痛点**：策划「公式」规范和真实 SVN 数据**两套并存且不一致**，纯靠脑子记必然乱；工具用「可编辑映射表（真相源）+ 公式辅助提示」双轨解决。

---

## 2. 痛点根因（为什么记不住）

| 现象 | 实例 | 说明 |
|---|---|---|
| 8 位 / 9 位两套制式并存 | 天命男 `50112101`（8位） vs 昆仑剑侠 `501121001`（9位） | 前缀后位数不同，肉眼难辨 |
| 公式 vs 真实数据不一致 | 公式写「时装编号 00–99（2位）」，真实用 `001–006`（3位） | 规范已过时/未同步 |
| 武器职业固定为 0（通用） | 昆仑剑侠武器 `501031001`（第4位=0） | 与身体/头发的职业位不同 |
| 性别编码混乱 | 8位制用 `01–49男 / 51–99女`（+50）；9位制用 `001–006` 顺序不显性别 | 无统一规则 |
| 大量缺口 / ❓ | SVN 表中很多头发/武器为空或 ❓ | 纯公式算不出，必须人工补 |

**结论**：不能只靠公式硬算。正确架构 = **可编辑的「ID ↔ 中文名」映射表做真相源** + **公式引擎只做辅助提示/自动预填**。

---

## 3. ID 规则提炼（核心技术，已验证）

### 3.1 结构拆解

```
9 位制： 5 0 1 | 职业(1) | 部位(1) | 套装(1) | 编号(3位)    例：5 0 1 1 2 1 0 0 1
8 位制： 5 0 1 | 职业(1) | 部位(1) | 套装(1) | 编号(2位)    例：5 0 1 1 2 1 0 1
          └前缀┘ └── 第4位 ──┘ └第5位┘  └─第6位─┘ └── 尾部=套装后所有位 ──┘
```
- **部位码**（第 5 位）：`1=头部/头发 2=身体 3=武器 4=翅膀 5=环绕 6=足迹`
- **职业码**（第 4 位）：`0=通用 1=昆仑剑侠 2=虬髯客 3=逍遥仙 4=月河郡主 5=拓跋影 6=娇俏精`
- **性别**：8位制 `编号 01–49=男 / 51–99=女`（女=男+50）；9位制 `001–006` 为角色序号，性别按角色设定（剑侠=男等）
- **套装**（第 6 位）：`1–9`，同角色不同外观

### 3.2 推导公式（辅助 / 仅改第 4、5 位，尾部不动）

给定主 ID（通常为**身体**，部位=2）：

| 目标 | 第4位(职业) | 第5位(部位) | 9位示例(501121001) | 8位示例(50112101) |
|---|---|---|---|---|
| 身体 | 不变 | →2 | `501121001` | `50112101` |
| 头发 | 不变 | →1 | `501111001` | `50111101` |
| 武器 | →0 | →3 | `501031001` | `50103101` |
| 翅膀 | 不变 | →4 | `501141001` | `50114101` |
| 环绕 | 不变 | →5 | `501151001` | `50115101` |
| 足迹 | 不变 | →6 | `501161001` | `50116101` |

> 验证：昆仑剑侠(剑) 身体 `501121001` → 头发 `501111001`（✓SVN）、武器 `501031001`（✓SVN）；天命男(剑) 身体 `50112101` → 头发 `50111101`（✓SVN `=C2-1000`）、武器 `50103101`（✓SVN）。

### 3.3 关联分组（需求 #3 的核心）

- **SetKey = (套装, 编号)**：同一角色同一时装的所有部位共享该键。
- 例如 `SetKey(套装=1, 编号=001)` 收录：昆仑剑侠(剑) 的身体/头发/武器/翅膀/环绕/足迹（存在的）。
- 输入任意 ID → 解析出 SetKey → 列出该组全部已知 ID（中文名 + 部位 + 提交状态）。

---

## 4. 功能设计（对应你的 5 条需求）

| # | 你的需求 | 实现方式 |
|---|---|---|
| 1 | 应用命名 + 项目文件夹 + GitHub 说明 | 命名 `MMY-ID-Generator`；脚手架 `D:/GitWork/MMY-ID-Generator`；中文 README 草稿见 §8 |
| 2 | 输主 ID → 自动列身体/头发/武器 | 公式引擎（§3.2）预填候选 → 与映射表交叉校验 → 命中显示中文名，未命中标「待补」 |
| 3 | 输任意 ID → 列关联 ID | 解析 SetKey → 拉出同组全部部位（§3.3） |
| 4 | ID↔中文映射 显示/编辑/迭代 | 可编辑表格（增/删/改/搜）；导入/导出 JSON；首次从 SVN 表自动初始化 seed |
| 5 | GUI 界面 | 单文件 `index.html`（内联 CSS/JS，浏览器打开即用） |

额外加分项（MVP 后可加）：公式反查（已知某部位 ID 反推主 ID）、SVN 提交状态可视化（☑️/❓）、模糊搜索、暗色模式。

---

## 5. 数据模型 `data.json`（真相源）

```json
{
  "meta": { "app": "MMY-ID-Generator", "version": "0.1.0",
            "source": "SVN时装职业装提交记录", "updated": "2026-08-20" },
  "records": [
    { "id": "501121001", "name": "昆仑剑侠(剑)", "part": "身体",
      "profession": "昆仑剑侠", "gender": "男", "suit": "1",
      "serial": "001", "type": "时装", "submitted": "✓", "note": "" },
    { "id": "501111001", "name": "昆仑剑侠(剑)头发", "part": "头发", ... },
    { "id": "501031001", "name": "昆仑剑侠(剑)武器", "part": "武器", ... }
  ]
}
```
- **SetKey** 由 app 运行时用 `suit + serial` 计算，不存冗余。
- **seed 数据**：实施时把 SVN 表（约 30+ 行含 ID 的：天命男/女、昆仑剑侠、虬髯客、逍遥仙、月河郡主、拓跋影、娇俏精 的身体/头发/武器 + 中文名 + 提交状态）一次性灌入。

---

## 6. 技术架构

- **零依赖、零构建**：单个 `index.html`，CSS/JS 全部内联；双击用浏览器打开即用，跨平台。
- **存储**：`data.json` 放同目录；应用内用浏览器 **File API** 做「导入 / 导出」（绕过 `file://` 直接 fetch 的限制），避免装服务器。可选 `localStorage` 做草稿缓存。
- **逻辑层**（原生 JS）：
  1. `parseID(id)` → `{prefix, profession, part, suit, serial, scheme}`
  2. `derive(part, profession)` → 改第 4/5 位生成候选 ID
  3. `groupBySetKey(records)` → 关联查询
  4. 编辑表 CRUD + 关键词搜索 + 从 Excel 导出的 JSON 初始化
- **可迭代**：每次新增角色，只需在映射表加几行（或导入新 JSON），无需改代码。

---

## 7. 项目结构（待创建于 `D:/GitWork/MMY-ID-Generator`）

```
MMY-ID-Generator/
├── index.html        # 单文件应用（GUI + 逻辑）
├── data.json         # ID↔中文名 映射（真相源，可编辑/导入导出）
├── README.md         # GitHub 中文说明
├── seed/             # 从两份 xlsx 抽出的初始数据脚本（可选）
└── docs/
    ├── report.md     # 本报告（中文）
    └── report.html   # 本报告（HTML，含 UI 线框）
```

---

## 8. GitHub README 草稿（中文，待生成文件）

```markdown
# MMY-ID-Generator

昆仑 / 轩辕剑 时装职业装「渲染输出 ID」对照、反查、关联与可迭代编辑工具。
专治"每次对渲染输出 ID 都看不懂、记不住"。

## 功能
- 输入主 ID（如昆仑剑侠(剑) `501121001`），自动列出 身体 / 头发 / 武器 ID 及中文名
- 输入任意 ID，列出同套装全部关联部位 ID
- ID ↔ 中文名 映射表的显示、搜索、增删改（可迭代，适配不断新增的角色）
- 数据存本地 `data.json`，支持导入 / 导出，零依赖、零联网

## 的快速开始
1. 用浏览器打开 `index.html`
2. 首次使用点「导入初始数据」加载 `data.json`
3. 在搜索框输入 ID 或中文名即可查询；点「编辑」维护映射

## ID 规则（速查）
- 结构：`501 + 职业(1) + 部位(1) + 套装(1) + 编号(2/3位)`
- 部位：1头 2身 3武 4翅 5环 6足 ｜ 职业：0通用 1昆仑剑侠 2虬髯客 3逍遥仙 4月河郡主 5拓跋影 6娇俏精
- 武器固定 职业→0、部位→3；头发 部位→1

## 数据来源
策划《ID规范》公式 + 《3D模型动画资源进度表》SVN 时装职业装提交记录。
```

---

## 9. 实施步骤（审批后执行）

1. **生成报告**：输出 `docs/report.md` + `docs/report.html`（含 UI 线框图），先给你看。
2. **抽 seed 数据**：脚本解析两份 xlsx → 生成 `data.json` 初始映射（身体/头发/武器 + 中文名 + 提交状态）。
3. **搭应用**：写 `index.html`（解析/推导/关联/编辑/导入导出 + GUI）。
4. **写 README**：落地 §8 内容到 `README.md`。
5. **自测清单**（你自测，我不代测）：主 ID 反查、任意 ID 关联、增删改、导入导出、8/9 位兼容。
6. **（可选）** 初始化 git 仓库、中文 commit、推送、打 tag。

---

## 10. 交付物清单
- `docs/report.md` / `docs/report.html` —— 中文设计报告（含 UI 线框）
- `index.html` —— 单文件 GUI 应用
- `data.json` —— 初始 ID↔中文名映射（来自 SVN 表）
- `README.md` —— GitHub 中文说明
- 项目文件夹 `D:/GitWork/MMY-ID-Generator` 脚手架

---
*待你审批后，我将先交付报告文件，再正式开发应用。*
